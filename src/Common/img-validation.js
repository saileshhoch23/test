const imageFormat = /\.(jpg|jpeg|png|bmp|svg)$/;

const imageMessage = "Allowed only png, jpg, jpeg, bmp, svg format";
const xlsxFormat = /\.(xlsx)$/;

const xlsxMessage = "Allowed only xlsx format";
export { imageFormat, imageMessage, xlsxFormat, xlsxMessage };